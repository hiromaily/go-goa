#local, heroku, docker, staging, production
environment = "heroku"

[jwt]
secret_code = "keys"

[mysql]
encrypted = false
host = "localhost"
port = 3306
dbname = "go-goa"
user = "guestuser"
pass = "secret123"
