// Code generated by SQLBoiler 3.7.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// TUserDislikeTech is an object representing the database table.
type TUserDislikeTech struct {
	ID        int         `boil:"id" json:"id" toml:"id" yaml:"id"`
	UserID    int         `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	TechID    int         `boil:"tech_id" json:"tech_id" toml:"tech_id" yaml:"tech_id"`
	IsDeleted null.String `boil:"is_deleted" json:"is_deleted,omitempty" toml:"is_deleted" yaml:"is_deleted,omitempty"`
	CreatedAt null.Time   `boil:"created_at" json:"created_at,omitempty" toml:"created_at" yaml:"created_at,omitempty"`
	UpdatedAt null.Time   `boil:"updated_at" json:"updated_at,omitempty" toml:"updated_at" yaml:"updated_at,omitempty"`

	R *tUserDislikeTechR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L tUserDislikeTechL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TUserDislikeTechColumns = struct {
	ID        string
	UserID    string
	TechID    string
	IsDeleted string
	CreatedAt string
	UpdatedAt string
}{
	ID:        "id",
	UserID:    "user_id",
	TechID:    "tech_id",
	IsDeleted: "is_deleted",
	CreatedAt: "created_at",
	UpdatedAt: "updated_at",
}

// Generated where

var TUserDislikeTechWhere = struct {
	ID        whereHelperint
	UserID    whereHelperint
	TechID    whereHelperint
	IsDeleted whereHelpernull_String
	CreatedAt whereHelpernull_Time
	UpdatedAt whereHelpernull_Time
}{
	ID:        whereHelperint{field: "`t_user_dislike_tech`.`id`"},
	UserID:    whereHelperint{field: "`t_user_dislike_tech`.`user_id`"},
	TechID:    whereHelperint{field: "`t_user_dislike_tech`.`tech_id`"},
	IsDeleted: whereHelpernull_String{field: "`t_user_dislike_tech`.`is_deleted`"},
	CreatedAt: whereHelpernull_Time{field: "`t_user_dislike_tech`.`created_at`"},
	UpdatedAt: whereHelpernull_Time{field: "`t_user_dislike_tech`.`updated_at`"},
}

// TUserDislikeTechRels is where relationship names are stored.
var TUserDislikeTechRels = struct {
}{}

// tUserDislikeTechR is where relationships are stored.
type tUserDislikeTechR struct {
}

// NewStruct creates a new relationship struct
func (*tUserDislikeTechR) NewStruct() *tUserDislikeTechR {
	return &tUserDislikeTechR{}
}

// tUserDislikeTechL is where Load methods for each relationship are stored.
type tUserDislikeTechL struct{}

var (
	tUserDislikeTechAllColumns            = []string{"id", "user_id", "tech_id", "is_deleted", "created_at", "updated_at"}
	tUserDislikeTechColumnsWithoutDefault = []string{"user_id", "tech_id"}
	tUserDislikeTechColumnsWithDefault    = []string{"id", "is_deleted", "created_at", "updated_at"}
	tUserDislikeTechPrimaryKeyColumns     = []string{"id"}
)

type (
	// TUserDislikeTechSlice is an alias for a slice of pointers to TUserDislikeTech.
	// This should generally be used opposed to []TUserDislikeTech.
	TUserDislikeTechSlice []*TUserDislikeTech

	tUserDislikeTechQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	tUserDislikeTechType                 = reflect.TypeOf(&TUserDislikeTech{})
	tUserDislikeTechMapping              = queries.MakeStructMapping(tUserDislikeTechType)
	tUserDislikeTechPrimaryKeyMapping, _ = queries.BindMapping(tUserDislikeTechType, tUserDislikeTechMapping, tUserDislikeTechPrimaryKeyColumns)
	tUserDislikeTechInsertCacheMut       sync.RWMutex
	tUserDislikeTechInsertCache          = make(map[string]insertCache)
	tUserDislikeTechUpdateCacheMut       sync.RWMutex
	tUserDislikeTechUpdateCache          = make(map[string]updateCache)
	tUserDislikeTechUpsertCacheMut       sync.RWMutex
	tUserDislikeTechUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single tUserDislikeTech record from the query.
func (q tUserDislikeTechQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TUserDislikeTech, error) {
	o := &TUserDislikeTech{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for t_user_dislike_tech")
	}

	return o, nil
}

// All returns all TUserDislikeTech records from the query.
func (q tUserDislikeTechQuery) All(ctx context.Context, exec boil.ContextExecutor) (TUserDislikeTechSlice, error) {
	var o []*TUserDislikeTech

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to TUserDislikeTech slice")
	}

	return o, nil
}

// Count returns the count of all TUserDislikeTech records in the query.
func (q tUserDislikeTechQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count t_user_dislike_tech rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q tUserDislikeTechQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if t_user_dislike_tech exists")
	}

	return count > 0, nil
}

// TUserDislikeTeches retrieves all the records using an executor.
func TUserDislikeTeches(mods ...qm.QueryMod) tUserDislikeTechQuery {
	mods = append(mods, qm.From("`t_user_dislike_tech`"))
	return tUserDislikeTechQuery{NewQuery(mods...)}
}

// FindTUserDislikeTech retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTUserDislikeTech(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*TUserDislikeTech, error) {
	tUserDislikeTechObj := &TUserDislikeTech{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `t_user_dislike_tech` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, tUserDislikeTechObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from t_user_dislike_tech")
	}

	return tUserDislikeTechObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TUserDislikeTech) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no t_user_dislike_tech provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
		if queries.MustTime(o.UpdatedAt).IsZero() {
			queries.SetScanner(&o.UpdatedAt, currTime)
		}
	}

	nzDefaults := queries.NonZeroDefaultSet(tUserDislikeTechColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	tUserDislikeTechInsertCacheMut.RLock()
	cache, cached := tUserDislikeTechInsertCache[key]
	tUserDislikeTechInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			tUserDislikeTechAllColumns,
			tUserDislikeTechColumnsWithDefault,
			tUserDislikeTechColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(tUserDislikeTechType, tUserDislikeTechMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(tUserDislikeTechType, tUserDislikeTechMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `t_user_dislike_tech` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `t_user_dislike_tech` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `t_user_dislike_tech` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, tUserDislikeTechPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into t_user_dislike_tech")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == tUserDislikeTechMapping["id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for t_user_dislike_tech")
	}

CacheNoHooks:
	if !cached {
		tUserDislikeTechInsertCacheMut.Lock()
		tUserDislikeTechInsertCache[key] = cache
		tUserDislikeTechInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the TUserDislikeTech.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TUserDislikeTech) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	var err error
	key := makeCacheKey(columns, nil)
	tUserDislikeTechUpdateCacheMut.RLock()
	cache, cached := tUserDislikeTechUpdateCache[key]
	tUserDislikeTechUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			tUserDislikeTechAllColumns,
			tUserDislikeTechPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update t_user_dislike_tech, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `t_user_dislike_tech` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, tUserDislikeTechPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(tUserDislikeTechType, tUserDislikeTechMapping, append(wl, tUserDislikeTechPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update t_user_dislike_tech row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for t_user_dislike_tech")
	}

	if !cached {
		tUserDislikeTechUpdateCacheMut.Lock()
		tUserDislikeTechUpdateCache[key] = cache
		tUserDislikeTechUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q tUserDislikeTechQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for t_user_dislike_tech")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for t_user_dislike_tech")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TUserDislikeTechSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tUserDislikeTechPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `t_user_dislike_tech` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tUserDislikeTechPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in tUserDislikeTech slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all tUserDislikeTech")
	}
	return rowsAff, nil
}

var mySQLTUserDislikeTechUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TUserDislikeTech) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no t_user_dislike_tech provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	nzDefaults := queries.NonZeroDefaultSet(tUserDislikeTechColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLTUserDislikeTechUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	tUserDislikeTechUpsertCacheMut.RLock()
	cache, cached := tUserDislikeTechUpsertCache[key]
	tUserDislikeTechUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			tUserDislikeTechAllColumns,
			tUserDislikeTechColumnsWithDefault,
			tUserDislikeTechColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			tUserDislikeTechAllColumns,
			tUserDislikeTechPrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("models: unable to upsert t_user_dislike_tech, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "t_user_dislike_tech", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `t_user_dislike_tech` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(tUserDislikeTechType, tUserDislikeTechMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(tUserDislikeTechType, tUserDislikeTechMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for t_user_dislike_tech")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == tUserDislikeTechMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(tUserDislikeTechType, tUserDislikeTechMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for t_user_dislike_tech")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for t_user_dislike_tech")
	}

CacheNoHooks:
	if !cached {
		tUserDislikeTechUpsertCacheMut.Lock()
		tUserDislikeTechUpsertCache[key] = cache
		tUserDislikeTechUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single TUserDislikeTech record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TUserDislikeTech) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no TUserDislikeTech provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), tUserDislikeTechPrimaryKeyMapping)
	sql := "DELETE FROM `t_user_dislike_tech` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from t_user_dislike_tech")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for t_user_dislike_tech")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q tUserDislikeTechQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no tUserDislikeTechQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from t_user_dislike_tech")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for t_user_dislike_tech")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TUserDislikeTechSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tUserDislikeTechPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `t_user_dislike_tech` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tUserDislikeTechPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from tUserDislikeTech slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for t_user_dislike_tech")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TUserDislikeTech) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTUserDislikeTech(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TUserDislikeTechSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TUserDislikeTechSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tUserDislikeTechPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `t_user_dislike_tech`.* FROM `t_user_dislike_tech` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tUserDislikeTechPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in TUserDislikeTechSlice")
	}

	*o = slice

	return nil
}

// TUserDislikeTechExists checks if the TUserDislikeTech row exists.
func TUserDislikeTechExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `t_user_dislike_tech` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if t_user_dislike_tech exists")
	}

	return exists, nil
}
